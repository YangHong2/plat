<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhlk.subcontract.dao.UserCouponsDao">
    <resultMap type="com.dhlk.entity.sub.UserCoupons" id="UserCouponsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="couponsId" column="coupons_id" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserCouponsMap">
        select
        id, user_id, coupons_id, status, create_time
        from dhlk_subcontract_plat.user_coupons
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserCouponsMap">
        select
        id, user_id, coupons_id, status, create_time
        from dhlk_subcontract_plat.user_coupons
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserCouponsMap">
        select
        id, user_id, coupons_id, status, create_time
        from dhlk_subcontract_plat.user_coupons
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="couponsId != null">
                and coupons_id = #{couponsId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into dhlk_subcontract_plat.user_coupons(user_id, coupons_id, status, create_time)
        values (#{userId}, #{couponsId}, #{status}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into dhlk_subcontract_plat.user_coupons(user_id, coupons_id, status, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.couponsId}, #{entity.status}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into dhlk_subcontract_plat.user_coupons(user_id, coupons_id, status, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.couponsId}, #{entity.status}, #{entity.createTime})
        </foreach>
        on duplicate key update
        user_id = values(user_id) , coupons_id = values(coupons_id) , status = values(status) , create_time =
        values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update dhlk_subcontract_plat.user_coupons
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="couponsId != null">
                coupons_id = #{couponsId},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from dhlk_subcontract_plat.user_coupons where id = #{id}
    </delete>
</mapper>

